import React from "react";

/**
 * ImageWrapper is going to be a wrapper around <img /> tag.
 * It will enforce a11y, standardized styling, and interactivity.
 * It will also offer retry logic.
 * And it will offer a loading state to display while image is loading. 
 */

export const ImageWrapper = () => {
  return <div>Image Wrapper</div>;
};
